# 구현 현황 (2026-08-09)

## ✅ 완료 — 실서버에서 종단 검증됨

| 영역 | 상태 | 비고 |
|---|---|---|
| 농민: 사진 → AI → SKU → 수량 → 등록 | ✅ | 실브라우저에서 전 단계 확인 |
| AI 파이프라인 5단계 모듈 분리 | ✅ | recognition / quality / sku / rule / writer |
| Vision provider 추상화 | ✅ | heuristic · openai · anthropic · mock |
| 로컬(무API) 판정 | ✅ | 24칸 색상 히스토그램 + 채도²·중심 가중 |
| AI 실패 폴백 (2지선다 → 전체선택) | ✅ | 인식 실패가 판매 불가로 이어지지 않음 |
| 신뢰도 게이트 + 1·2순위 격차 규칙 | ✅ | auto / choose / manual |
| 표준 SKU 엔진 (가격 유일 출처) | ✅ | 클라이언트 가격 주입 차단 테스트 있음 |
| 상품 문안 자동 생성 | ✅ | DB값+사진근거만, 금액 미포함 검증 |
| 소비자 매장·상세·장바구니·주문 | ✅ | 주문 시 재고 차감·완판 자동 처리 |
| 신뢰 표시 (생산자·수확일·AI근거·검수상태) | ✅ | 상세 페이지 타임라인 |
| 거점 대시보드 (검수 통과/조정/반려) | ✅ | 카운터·상태 전이 실확인 |
| 정산 (수수료 8% 가정값 명시) | ✅ | 주문 트랜잭션에 포함 |
| 역할 접근 제어 (서버측) | ✅ | 뮤테이션 테스트로 가드 실효성 확인 |
| TTS 음성 안내 / STT 수량 입력 | ✅ | 미지원 브라우저에서 자동 숨김 |
| PWA (manifest + 셸 캐시 SW) | ✅ | API는 캐시 안 함 |
| 데모 모드·리셋 (`/demo`) | ✅ | 리셋은 로컬 요청만 허용 |
| 테스트 75개 | ✅ | 3회 뮤테이션(재고·권한·폴백)으로 빨간불 확인 |

## ⚠️ 알려진 한계 (정직하게)

- **로컬 판정은 딥러닝이 아니다.** 색·질감 규칙이라 비슷한 색 품목(배↔양파↔감자)은 실물 사진에서 choose 모드로 자주 빠진다. 화면에 판정 방식을 그대로 표기하며, 이는 CV 모델(AI 허브 QC 데이터 학습)로 교체되는 자리다.
- **openai/anthropic provider는 실키로 미검증.** 스키마 검증·타임아웃·폴백 경로는 테스트로 덮었지만, 실제 API 응답 형태 변화는 키를 넣어 확인 필요.
- **결제 없음.** 주문은 '결제완료' 상태로 기록만 된다(화면에 명시).
- **분석 결과가 서버 메모리라 재시작하면 진행 중이던 등록은 만료된다.** 등록 완료된 상품은 영향 없음.
- **STT는 브라우저(Chrome 계열) 의존.** 실패해도 +/- 버튼으로 완결.
- **거점 다중화 미구현.** 거점 1곳 가정의 단일 대시보드. `hubs` 테이블은 3곳 시드됨.
- **수산물은 DB 자리만.** MVP는 농산물 8품목.

## ❌ 미구현 (로드맵)

- 실결제(PG)·배송 추적 연동
- CV 모델 학습·서빙 (VisionProvider 구현 추가로 접합)
- 아파트 공동구매 / B2B 견적
- 판매채널 추천·수요예측 (데이터 축적 후)
