# 구현 현황 (2026-08-09)

## 🔍 Codex 교차검증 반영 (2026-08-09)

읽기전용 샌드박스로 전체를 적대적 검토시켜 22건을 받았고, 실제 파일과 대조해 **17건을 수정**했습니다.
테스트 75 → **103개**로 늘었고, 새 가드 3개(분석 소진·등급 허용목록·확정등급 분리)는 일부러 되돌려 빨간불을 확인했습니다.

| 지적 | 조치 |
|---|---|
| 같은 `analysisId` 로 중복 매물 등록 | 등록 시 소진 처리(재시도·동시요청 → 409) |
| 품목 변경 시 이전 품종("신고배")이 새 상품 제목에 잔류 | 품목 변경 시 품종·등급·근거 초기화, 문안 재작성 |
| `harvestedOn` 만 바뀌면 문안이 옛 날짜 유지 | 날짜 변경도 재작성 조건에 포함 + 미래 날짜 거부 |
| 데모 초기화가 진행 중 요청의 DB 핸들을 닫음 | 파일 삭제 대신 표만 비우는 방식으로 변경 |
| `JSON.parse as T` 로 잘못된 본문이 500 | 객체가 아니면 400, 필드 타입 방어 |
| `limit=abc` → `LIMIT NaN` 으로 500 | 숫자 검증 + 정수 강제 |
| 주문번호 4자리(하루 1,000건에 사실상 충돌) | 6자리 + 충돌 시 재발급 |
| 검수 기록과 상태 변경이 별도 문장 | 한 트랜잭션으로 묶음 |
| 잘못 인코딩된 URL(`/%`)로 프로세스 종료 가능 | `decodeURIComponent` 예외 처리 |
| 선언 MIME 만 믿고 비이미지 저장 | 매직 넘버 검사(JPEG/PNG/WebP) |
| 거점 담당자가 전국 물량 처리 가능 | `users.hub_id` 도입, 목록·검수·상태변경 모두 소속 거점으로 제한 |
| 상태를 임의로 되돌리거나 건너뛰기 가능 | 전이 규칙표로 전진만 허용 |
| **"검수 통과"가 AI 참고값을 확정으로 승격** | 담당자가 등급을 직접 선택해야 확정(미선택 시 400) |
| **확정 등급이 AI 참고값 칸을 덮어씀** | `confirmed_quality` 컬럼 분리, 소비자 화면에 "거점 확정 등급"으로 별도 표기 |
| `confidence:"0.99"` 문자열이 숫자로 강제 변환 | 숫자 아니면 거부 |
| 무농약·잔류농약 등 검증 불가 클레임이 문안에 유입 | 금지 클레임 필터 + 품종 길이 제한 |
| 정상 자동인식도 `manual` 로 감사 기록 | 실제 품목 변경 시에만 override 기록 |
| 분석 실패 시 폴백 화면이 막다른 길 | 사진을 들고 있다가 품목 선택과 함께 재분석 |
| 외부 provider 실패를 농민 화면이 숨김 | 강등 사유를 결과 화면에 표기 |
| 메모리·디스크 무제한 증가 | 사용자별 보관 상한 + 초기화 시 업로드 정리 |

**남긴 것(의도적)**: ① 비밀번호 없는 데모 로그인 — MVP 인증은 시연용이며 실서비스는 본인확인으로 대체(README 명시). ② 클라이언트가 보내는 이미지 특징값 — 서버가 JPEG 를 디코딩하려면 의존성이 필요하고, 농민이 자기 상품을 속일 동기가 없으며 어차피 직접 선택 경로가 있어 이득이 없음. 다만 **신뢰 경계임을 문서에 명시**하고 CV 모델 도입 시 서버 추론으로 옮길 자리로 표시.

## ✅ 완료 — 실서버에서 종단 검증됨

| 영역 | 상태 | 비고 |
|---|---|---|
| 농민: 사진 → AI → SKU → 수량 → 등록 | ✅ | 실브라우저에서 전 단계 확인 |
| AI 파이프라인 5단계 모듈 분리 | ✅ | recognition / quality / sku / rule / writer |
| Vision provider 추상화 | ✅ | heuristic · openai · anthropic · mock |
| 로컬(무API) 판정 | ✅ | 24칸 색상 히스토그램 + 채도²·중심 가중 |
| AI 실패 폴백 (2지선다 → 전체선택) | ✅ | 인식 실패가 판매 불가로 이어지지 않음 |
| 신뢰도 게이트 + 1·2순위 격차 규칙 | ✅ | auto / choose / manual |
| 표준 SKU 엔진 (가격 유일 출처) | ✅ | 클라이언트 가격 주입 차단 테스트 있음 |
| 상품 문안 자동 생성 | ✅ | DB값+사진근거만, 금액 미포함 검증 |
| 소비자 매장·상세·장바구니·주문 | ✅ | 주문 시 재고 차감·완판 자동 처리 |
| 신뢰 표시 (생산자·수확일·AI근거·검수상태) | ✅ | 상세 페이지 타임라인 |
| 거점 대시보드 (검수 통과/조정/반려) | ✅ | 카운터·상태 전이 실확인 |
| 정산 (수수료 8% 가정값 명시) | ✅ | 주문 트랜잭션에 포함 |
| 역할 접근 제어 (서버측) | ✅ | 뮤테이션 테스트로 가드 실효성 확인 |
| TTS 음성 안내 / STT 수량 입력 | ✅ | 미지원 브라우저에서 자동 숨김 |
| PWA (manifest + 셸 캐시 SW) | ✅ | API는 캐시 안 함 |
| 데모 모드·리셋 (`/demo`) | ✅ | 리셋은 로컬 요청만 허용 |
| 테스트 75개 | ✅ | 3회 뮤테이션(재고·권한·폴백)으로 빨간불 확인 |

## ⚠️ 알려진 한계 (정직하게)

- **로컬 판정은 딥러닝이 아니다.** 색·질감 규칙이라 비슷한 색 품목(배↔양파↔감자)은 실물 사진에서 choose 모드로 자주 빠진다. 화면에 판정 방식을 그대로 표기하며, 이는 CV 모델(AI 허브 QC 데이터 학습)로 교체되는 자리다.
- **프로토타입 색표를 실측값으로 교체했다(2026-08-13).** 기존 `heuristic.ts` 의 `PROTOTYPES` 는 눈대중 색표라 실제 상품 사진과 어긋나 있었다. 진짜 배(대표 색상각 36.6°)가 표의 양파 자리(38°)에 떨어져 **배를 양파로 판정**하는 등, 참조 사진 8장 중 1순위 적중이 4/8뿐이었다. 같은 추출기(`features.js`)로 8장을 측정한 값으로 표를 갈아 **8/8** 이 됐다. 다만 이 값은 **흰 배경 스튜디오풍 상품컷 기준**이라, 논밭에서 찍은 폰 사진의 성능은 별개다. 신뢰도 상한 0.86 과 choose/manual 게이트는 그대로 두었다.
- **openai/anthropic provider는 실키로 미검증.** 스키마 검증·타임아웃·폴백 경로는 테스트로 덮었지만, 실제 API 응답 형태 변화는 키를 넣어 확인 필요.
- **결제 없음.** 주문은 '결제완료' 상태로 기록만 된다(화면에 명시).
- **분석 결과가 서버 메모리라 재시작하면 진행 중이던 등록은 만료된다.** 등록 완료된 상품은 영향 없음.
- **STT는 브라우저(Chrome 계열) 의존.** 실패해도 +/- 버튼으로 완결.
- **이미지 특징값을 클라이언트가 계산한다.** 서버는 그 값이 실제 사진과 일치하는지 대조하지 못한다(신뢰 경계). 조작하면 로컬 판정 결과를 유도할 수 있으나, 농민에게는 어차피 품목 직접 선택 경로가 있어 얻는 이득이 없다. CV 모델 도입 시 서버 추론으로 옮긴다.
- **인증이 데모용이다.** 비밀번호 없이 계정을 고르는 방식이라 보안 경계로는 무효다. 역할 검사·세션 서명·거점 범위 제한은 실제로 동작하므로, 로그인만 본인확인으로 교체하면 된다. 공개 배포 시 `SESSION_SECRET` 변경 필수.
- **수산물은 DB 자리만.** MVP는 농산물 8품목.

## 🔬 CV 모델 (진행 중, 2026-08-09)

AI 허브 「농산물 품질(QC) 이미지」(datasetkey 149)로 품목·등급 모델을 학습한다.
데이터 실측·수령 절차·한계는 [docs/aihub-dataset.md](docs/aihub-dataset.md).

| 단계 | 도구 | 상태 |
|---|---|---|
| 수집 (국내 PC 필수 — 해외 IP 차단) | `tools/aihub_ingest.py` | ✅ 122,425장 / 1,071MB (4.5시간) |
| 검증 + 업로드 패키징 | `tools/pack_for_colab.py` | ✅ |
| 학습 (Colab GPU, EfficientNet-B0 6에폭) | `notebooks/onfarm_train_colab.ipynb` | ✅ |
| 서비스 접합 (`models/` + `AI_PROVIDER=cnn`) | `src/ai/providers/cnn.ts` | ✅ 실사진 종단 검증 |

**학습 결과 (검증 개체 200, 스튜디오 데이터 기준)**
- 품목: 개체 단위 **1.000** — 단, 같은 스튜디오 조건 재측정이므로 실사용 수치가 아니다.
  서버 경로(ONNX+자체 전처리) 재검증에서도 500장/196개체 전량 일치(`tools/verify_model.mjs`).
- 등급: 개체 단위 **0.705** > 중량 단독 기준선 0.580 → 등급 출력 사용 가능 판정.
  단 품목별 편차가 크다(감자 0.944 ~ 양파 0.434). 화면에는 계속 '참고 판정'으로만.
- 확신도: 평균 0.963, 틀렸을 때 0.666 → 신뢰도 게이트가 유효하게 동작할 여지.
- **대외 표기 원칙: '품목 100%' 단독 표기 금지.** 반드시 '공개데이터 검증셋(스튜디오 촬영) 기준,
  실사용 폰 사진 성능은 별도 측정 예정'을 병기한다.

**접합 방식**: `onnxruntime-node` 를 **선택적 의존성**으로 둔다. 설치돼 있고 `models/` 에
`onfarm_qc.onnx` + `metadata.json` 이 있으면 `AI_PROVIDER=cnn` 으로 뜨고, 없으면
서버 기동을 막지 않고 로컬 규칙 판정으로 내려앉는다(사유는 화면 배너에 표기).
서버에 JPEG 디코더를 들이지 않으려고 브라우저가 224×224 RGB 픽셀을 함께 보낸다 —
`features` 와 같은 신뢰 경계다.

**모델 출력을 그대로 믿지 않는다**:
- 확신도는 `metadata.val_object_level.item`(개체 단위 검증 정확도)을 상한으로 자른다.
- 등급은 `weight_only_grade_baseline`(중량만 보는 기준선)을 넘을 때만 화면에 쓴다.
  못 넘으면 품목만 쓰고 등급은 거점 실물 검수에 맡긴다.

## ❌ 미구현 (로드맵)

- 실결제(PG)·배송 추적 연동
- 아파트 공동구매 / B2B 견적
- 판매채널 추천·수요예측 (데이터 축적 후)
